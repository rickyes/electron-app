{
  "name": "electron-app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"start": "electron .",
	"packager": "electron-packager ./ electron-app --all --out ./OutApp --version 1.4.0 --overwrite --icon=./icon.ico"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-settings": "^3.1.1",
    "electron-shortcut-normalizer": "^1.0.0",
    "highlight.js": "^9.12.0"
  }
}
